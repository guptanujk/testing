export enum PMTool {
    JIRA = "JIRA",
    ADO = "ADO",
    Rally = "Rally",
    ACTIVE_DIRECTORY = "ACTIVE_DIRECTORY"
}

export enum PMAuthType {
    BEARERTOKEN = "Bearer Token",
    BASICAUTH = "Basic Auth",
    DIGESTAUTH = "Digest Auth",
    OAUTH = "OAuth",
    OAUTH_AD = "OAuth AD"
}

export enum PMManagementType {
    PEOPLE = "People",
    PROJECTS = "Projects",
}
